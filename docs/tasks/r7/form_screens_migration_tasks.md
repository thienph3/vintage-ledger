# Tasks: Form Screens Style Migration

| # | Task | File(s) | Detail |
|---|------|---------|--------|
| 1 | budget_form_screen | `budget_form_screen.dart` | Colors → `primary`/`expense`. Radius → 16. Section titles → `titleSmall` |
| 2 | budget_list_screen | `budget_list_screen.dart` | Progress bar colors → `income`/`expense`/`primary`. Radius → 16. Loading → shimmer |
| 3 | monthly_insight_screen | `monthly_insight_screen.dart` | Colors → AppColors. Casual tone. Radius → 16 |
| 4 | category_form_screen | `category_form_screen.dart` | Colors → `primary`. Radius → 16 |
| 5 | category_list_screen | `category_list_screen.dart` | Colors → `primary`/`expense`/`income`. Radius → 16. Loading → shimmer |
| 6 | recurring_form_screen | `recurring_form_screen.dart` | Colors → `primary`. Radius → 16 |
| 7 | recurring_list_screen | `recurring_list_screen.dart` | Colors → `primary`. Radius → 16. Loading → shimmer |
| 8 | family_detail_screen | `family_detail_screen.dart` | Colors → `primary`/`expense`/`textPrimary`. Radius → 16. Avatar soft style |
| 9 | family_form_screen | `family_form_screen.dart` | Colors → `primary`. Radius → 16 |
| 10 | account_picker_screen | `account_picker_screen.dart` | Colors → `primary`. Radius → 16. Loading → shimmer |
| 11 | transaction_form_screen | `transaction_form_screen.dart` | Colors → `primary`. Radius → 16. Member dropdown soft style |
